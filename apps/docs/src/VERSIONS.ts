export const CORE_VERSIONS = ["0.1.0"].reverse();

export const LATEST_CORE_CHANGELOG_URL = `/docs/changelog/${CORE_VERSIONS[0].replaceAll(".", "-")}`;

export const LATEST_CORE_VERSION_NAME = `v${CORE_VERSIONS[0]}`;
